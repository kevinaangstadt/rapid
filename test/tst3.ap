/* Test 3 */
macro test3(String s) {
    Counter cnt;
    foreach (char c : s) {
        if(!(c == input() && c != input()))
            cnt.count();
    }
    
    if(cnt < (16-9))
        report;
    cnt.reset();
}

network () {
    test3("abcdef");
}
