macro gappy() {
    if('a' == input() ) {
        either {
            '*' == input();
               
        } orelse {
            '*' == input() && '*' == input();
                
        }
        if('b' == input() && 'c' == input())
            report;
    }
}

network () {
    gappy();
}
